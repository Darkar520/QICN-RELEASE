# Completion Decision

## Decision

`BASECORE_FORMAL_NORMALIZATION_APPLIED`

## Meaning

The requested normalization pass is complete at source level:

- the active document has been renamed to BaseCore at the release surface;
- the typed computable model is repaired;
- the non-collapse theorem uses a parameterwise anti-constant hypothesis;
- the state spectral section no longer carries a unit eigenvalue under strict contraction;
- inverse-limit nonlocality is conditioned on NFD;
- rigidity and non-simulability are conditionalized;
- Papers 7-9 remain downstream.

## Final status statement

The active mathematical base document is now `BASECORE.tex`, and `BASECORE.pdf` is the synchronized compiled artifact. `CANONICAL_CORE.tex` remains only as an explicit deprecated compatibility wrapper.
