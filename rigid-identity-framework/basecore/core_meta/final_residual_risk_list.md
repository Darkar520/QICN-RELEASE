# Final Residual Risk List

## Residual 1: page-count ceiling

The final canonical core stops at `64` pages, not `100+`.

Reason:

- the approved intermediate absorption policy was enforced;
- only theorem-level or structurally necessary material from Papers 1-6 was admitted;
- padding to exceed 100 pages would have violated the anti-redundancy rule.

## Residual 2: typographic warnings

The final log still contains minor box warnings:

- `3` overfull boxes
- `2` underfull boxes

These are layout-quality issues, not theorem-integrity or bibliography-integrity issues.

## Residual 3: model-specific content inside an otherwise canonical core

Sections 2 and 6 still contain model-specific and parameter-space material inherited from absorbed formal papers. This is allowed under the approved intermediate strategy, but a future stricter edition could move some of that material into a companion technical supplement.

## Residual 4: dirty workspace outside scope

The repository root contains many unrelated modified or untracked files. They were preserved untouched to avoid accidental regression outside the canonical-core task.
