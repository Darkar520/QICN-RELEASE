# Non-Redundancy Audit

## What was removed or normalized

- duplicate active section heading in `04_regime_constraints_absorbed.tex` was collapsed to a single `Extended Proofs` section;
- duplicate labels created during absorption in `07_operational_criterion_absorbed.tex` were renamed to unique canonical identifiers;
- the appendix-level `\printbibliography` was removed so bibliography ownership stays with the master file;
- README was rewritten so the expanded source tree is described once, as the active canonical structure.

## Structural anti-redundancy result

The current source tree is no longer a monolithic overlap of old core prose plus absorbed paper fragments. It is partitioned into:

1. foundation
2. model and spectral extensions
3. rigidity and uniqueness structure
4. regime constraints
5. null-regime exclusion
6. structural-class extensions
7. operational criterion
8. claim boundary and falsation grammar
9. canonical ledgers
10. counterexample appendix

## Residual redundancy judgment

No remaining duplicate active headings, duplicate bibliography printers, or duplicate absorbed labels were found in the audited core files.

There remains one acceptable form of deliberate overlap:

- Section 7 restates selected invariant content in operational form because those objects are needed to define certification, transfer, rupture, and class transitions. This is not counted as rhetorical duplication because it introduces new operators, margins, and admissibility structure.

## Honesty note

The document is still not a 100+ page book. That is a feature of the anti-redundancy filter, not a failure of execution.
