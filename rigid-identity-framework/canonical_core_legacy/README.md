# Canonical Core Legacy Package

This folder preserves the earlier 64-page Canonical Core package as a historical paper package.

## Status

- Historical package only
- Not the active source-of-truth for the base layer
- Superseded by `basecore/`

## Contents

- `CANONICAL_CORE.pdf` legacy compiled artifact
- `CANONICAL_CORE.tex` legacy source surface for the historical package
- `source_snapshots/` preserved source snapshots
- `build_logs/` preserved historical build logs
- `build_workspace/` preserved historical build workspace directory

Use this folder for archival comparison or citation history, not as the active base layer for ongoing edits.
