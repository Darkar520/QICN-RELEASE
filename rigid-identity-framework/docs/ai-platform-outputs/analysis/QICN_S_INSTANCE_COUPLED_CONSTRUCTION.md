# QICN Coupled S-Instance Construction Attempt

Status: `NON_CANONICAL_SPECULATIVE_INTERNAL_COUPLED_ATTEMPT`

Date: 2026-06-19

Human review: `REQUIRED`

Human curated status: `not_reviewed`

Boundary: this is an internal model-conformity attempt against
`QICN_S_INSTANCE_GENUINENESS_CRITERIA.md`. It does not modify the prior product
attempt (`QICN_S_INSTANCE_CONSTRUCTION.md`). It is not external validation, not
a real-world `C_op` claim, and not a claim about consciousness, identity,
subjectivity, phenomenality, CCR, `I_int`, or no-vacuity.

## Criteria Document Used

Precondition:

- `docs/ai-platform-outputs/analysis/QICN_S_INSTANCE_GENUINENESS_CRITERIA.md`

The coupled attempt is scored against the same `PASS_*` and prohibited toy
patterns. No criterion is relaxed.

## Concrete Coupled Carrier

State space:

```text
X := R^2
d_X := Euclidean norm metric
```

Write states as vectors `x in R^2`.

Linear part:

```text
rho := 1/4
theta := pi/3
K := rho * R(theta)
```

where `R(theta)` is the real rotation matrix. Thus:

```text
||K|| = rho = 1/4 < 1
spec_C(K) = { rho * exp(i theta), rho * exp(-i theta) }
```

Because `theta` is not a multiple of `pi`, the real linear map has no real
one-dimensional invariant eigenspace. This is the intended exact
linear-factorization obstruction. The Lean file
`QICNRotationSpectral.lean` now mechanizes this exact obstruction for the
complex-plane model of the rotation-contraction block.

Interventions:

```text
U := {u0,u1,u2}
u0 := (1,0)
u1 := rotation by 2*pi/3 of u0
u2 := rotation by 4*pi/3 of u0
Gamma(ui) := ui
```

`U` is compact and non-singleton with the discrete topology. `Gamma` is
continuous.

Projection carrier:

```text
s := closed disk { x : ||x|| <= 2 }
Phi_u(x) := convexProjection s (K x + Gamma(u))
```

On the support below, projection is inactive because:

```text
||K x + Gamma(u)|| <= (1/4)*2 + 1 = 3/2 < 2.
```

Common support:

```text
A := { x : 1/2 <= ||x|| <= 2 }
```

This annulus is compact, connected, nonempty, and non-singleton. It is the same
support for all six invariant checks.

Forward invariance:

For `x in A` and every `u in U`:

```text
upper bound: ||Kx + Gamma(u)|| <= 3/2 <= 2
lower bound: ||Kx + Gamma(u)|| >= 1 - (1/4)*2 = 1/2
```

so `Phi_u(A) subset A`.

Collapse set:

```text
Collapse := { x : ||x|| <= 1/4 }
```

Then:

```text
dist(A, Collapse) >= 1/4.
```

Readouts:

```text
R := { r_id, r_norm, r_phase }
r_id(x) := x
r_norm(x) := ||x||
r_phase(x) := x / ||x||   on A
```

These readouts do not isolate a decoupled coordinate. They observe the coupled
state vector, radius, and phase.

Causal structure:

```text
C := directed intervention-response structure generated by K, Gamma, and R.
```

Because `K` rotates and contracts, each coordinate at the next step depends on
both coordinates at the previous step.

## Lean Mechanization Surface

Lean file:

- `docs/ai-platform-outputs/formal/lean/QICNLean/QICNSCoupledInstance.lean`
- `docs/ai-platform-outputs/formal/lean/QICNLean/QICNRotationSpectral.lean`

Mechanized reusable facts:

```text
NoNontrivialInvariantRealSubspace
coupled_fixedPoint_isFixedPt
coupled_fixedPoint_unique
coupled_attractor_family_isCompact
coupled_fixedPoint_selector_continuous
coupled_blocks_exact_linear_factorization
complex_mul_no_nontrivial_invariant_real_subspace
rotation_contraction_no_invariant_line
```

Lean status:

- The projected-affine fixed-point/compactness/continuity carrier is verified.
- The exact-factorization obstruction is represented abstractly as
  `NoNontrivialInvariantRealSubspace K`.
- The exact no-real-invariant-line spectral fact is mechanized as
  `rotation_contraction_no_invariant_line` for the complex-plane
  rotation-contraction scalar `1/8 + (sqrt 3)/8 i`.
- The approximate nonfactorization margin `delta_int>0` is not mechanized.

Build result:

```text
lake build: EXIT=0
Build completed successfully (2299 jobs).
sorry/admit/axiom grep: COUNT=0
```

`#print axioms`:

```text
'QICNLean.coupled_fixedPoint_unique' depends on axioms: [propext, Classical.choice, Quot.sound]
'QICNLean.coupled_attractor_family_isCompact' depends on axioms: [propext, Classical.choice, Quot.sound]
'QICNLean.coupled_fixedPoint_selector_continuous' depends on axioms: [propext, Classical.choice, Quot.sound]
'QICNLean.coupled_blocks_exact_linear_factorization' does not depend on any axioms
'QICNLean.rotation_contraction_no_invariant_line' depends on axioms: [propext, Classical.choice, Quot.sound]
```

## Admissible Factorization Class for Iint

The factorization class is not empty.

An admissible continuous factorization candidate consists of:

1. A real linear direct-sum decomposition
   `R^2 = L1 direct_sum L2`, where `L1` and `L2` are nonzero real
   one-dimensional subspaces.
2. A split readout family `R = R1 disjoint_union R2` after an admissible real
   change of basis.
3. A split causal structure `C = C1 disjoint_union C2`.
4. Component dynamics whose product update reproduces admissible histories on
   `A` below a claimed error threshold.

Exact product factorization would require both `L1` and `L2` to be invariant
under the linear part `K`. For `rho * R(pi/3)`, no real invariant line exists.
Therefore exact linear product factorization is structurally blocked.

What is not closed:

- A uniform lower bound `delta_int>0` against all approximate factorizations.
- A compactness/normalization argument over the factorization search space.

Thus `Iint` receives a real opportunity here, but is not certified.

## Invariant Scoring Against PASS_* Criteria

### Iper - CERTIFIED_INTERNAL

Margin:

```text
delta_per = 1/4
```

Reason:

- `A` is nonempty, compact, connected, and non-singleton.
- `Phi_u(A) subset A` for every `u in U`.
- `A` contains the fixed-point family; by Banach, each `u` has a unique fixed
  point and its norm lies between `1/(1+rho)=4/5` and `1/(1-rho)=4/3`.
- `A` is separated from the collapse set by at least `1/4`.

Toy patterns avoided:

- Criteria pattern 2: dynamics is not globally constant.
- Pattern 3: dynamics is not identity-only.
- Pattern 4: support is not singleton.
- Pattern 11: `U` is not singleton.
- Pattern 12: collapse is excluded by an explicit distance margin.
- Pattern 15: the same `A` is used throughout.

### Iri - CERTIFIED_INTERNAL_LIMITED_ALTERNATIVE_CLASS

Margin:

```text
delta_ri = 1
```

Identity object:

```text
Id_S := u |-> f_u*
```

where `f_u*` is the Banach fixed point of `Phi_u`.

Alternative class:

```text
phase-shifted fixed-point selectors by angular offsets >= pi/2
```

Reason:

- For each `u`, strict contraction gives a unique fixed point.
- The alternative class is nonempty and pre-registered; it is not the empty
  set.
- The fixed-point selector is an invertible linear image of the three forcing
  directions, with pairwise angular separation inherited from the interventions.
- The conservative gap `delta_ri=1` is below the analytic separation implied by
  `rho=1/4` and the `120 degree` intervention spacing.

Limitation:

- This is not a universal theorem over every possible identity semantics. It is
  internal certification against the specified admissible alternative class.

Toy patterns avoided:

- Pattern 5: alternatives are not empty.
- Pattern 8: readouts are not all-zero.
- Pattern 10: the margin is explicit, not arbitrary.

### Iint - DEFERRED

Status:

```text
IINT_STATUS: DEFERRED
```

Positive progress:

- The admissible factorization class is explicitly defined.
- Exact real-linear product splitting is obstructed by the absence of real
  invariant one-dimensional subspaces for `rho * R(pi/3)`.
- Readouts are coupled/full-state rather than a single decoupled coordinate.

Missing burden:

- No certified `delta_int>0` lower bound against all approximate admissible
  factorizations.
- No proof that the exact no-invariant-line obstruction yields a uniform
  positive error against all admissible approximate continuous factorizations.
- No proof that every admissible approximate split fails to reproduce histories
  below a positive margin.

Toy patterns not yet fully neutralized:

- Pattern 1 is addressed for exact linear splitting, but not for approximate
  factorization below `delta_int`.
- Pattern 6 is avoided because the factorization class is defined and nonempty.
- Pattern 10 remains unresolved for `delta_int`.

### Icont - CERTIFIED_INTERNAL

Margin:

```text
delta_cont = 1/4
```

Regime space:

```text
E := S^1 with geodesic metric
```

Assignment:

```text
Pi_S(x) := x / ||x||
```

Reason:

- `A` is connected, so the construction does not use a discrete two-point
  regime trick.
- `S^1` is compact and complete.
- `Pi_S` is continuous on `A` because `||x|| >= 1/2`.
- The positive margin `delta_cont=1/4` is the radial guard to the singular
  point/collapse neighborhood; perturbations smaller than this cannot force
  `Pi_S` to become undefined.
- `Pi_S` is not constant on the annulus.

Toy patterns avoided:

- Pattern 7: regime map is not constant.
- Pattern 10: positive margin is explicit.
- Pattern 15: same support `A`.

### Idiff - CERTIFIED_INTERNAL

Margin:

```text
delta_diff = 1/2
```

Witness:

```text
x := (1,0)
y := (-1,0)
Read* := { r_id }
T := 1
schedule := any fixed u in U
```

Because the same intervention cancels in the difference:

```text
Phi_u(x) - Phi_u(y) = K(x-y)
||K(x-y)|| = (1/4)*2 = 1/2.
```

Toy patterns avoided:

- Pattern 8: readout is full-state and nondegenerate.
- Pattern 13: separation is deterministic, not noise-driven.
- Pattern 4: support contains distinct states.

### Ileg - CERTIFIED_INTERNAL

Margin:

```text
delta_leg = 1/4
```

Class set:

```text
Class_S := {C0,C1,C2}
```

Decoder:

```text
Dec_S(window) := nearest intervention center Gamma(ui) to the latest full-state
readout r_id(x_t)
```

Reason:

- After intervention `ui`, every state in `A` maps within distance at most
  `rho * 2 = 1/2` of `Gamma(ui)`.
- The three intervention centers are pairwise separated by `sqrt(3)`.
- Since `sqrt(3) - 1 > 1/2`, the nearest-center decoder has a positive
  robustness margin; `delta_leg=1/4` is conservative.
- Off-target interventions land near different centers, so negative controls
  do not reproduce the target signature.
- Compression to nearest-center class preserves the three-class partition.
- The decoder uses the latest full-state readout, not construction labels,
  state IDs, or intervention IDs.

Toy patterns avoided:

- Pattern 8: readout is not all-zero or duplicated.
- Pattern 9: decoder does not memorize labels; it reads state geometry.
- Pattern 10: explicit margin.
- Pattern 11: non-singleton intervention set.
- Pattern 14: partition is auditable and compressible.

## Summary Table

| Invariant | Status | Margin | Anti-toy verdict |
|---|---|---:|---|
| `Iper` | `CERTIFIED_INTERNAL` | `1/4` | Connected non-singleton invariant annulus, separated collapse set. |
| `Iri` | `CERTIFIED_INTERNAL_LIMITED_ALTERNATIVE_CLASS` | `1` | Nonempty phase-shift alternative class; not universal over all semantics. |
| `Iint` | `DEFERRED` | none | Exact linear splitting obstructed; approximate `delta_int` not certified. |
| `Icont` | `CERTIFIED_INTERNAL` | `1/4` | Continuous phase map to complete `S^1`, not discrete trick. |
| `Idiff` | `CERTIFIED_INTERNAL` | `1/2` | Full-state deterministic history separation. |
| `Ileg` | `CERTIFIED_INTERNAL` | `1/4` | Nearest-center decoder with controls and negative controls. |

## Verdict

```text
S_COUPLED_CERTIFIED_INTERNAL:
  Iper(delta=1/4), Iri(delta=1; limited alternative class),
  Icont(delta=1/4), Idiff(delta=1/2), Ileg(delta=1/4)

S_COUPLED_DEFERRED:
  Iint -- exact factorization is structurally obstructed, but no uniform
  approximate-factorization margin delta_int is certified.

IINT_STATUS:
  DEFERRED(exact no-real-invariant-line obstruction is mechanized; uniform
  delta_int over all admissible approximate continuous factorizations is still
  missing)

FULL_COP_MEMBERSHIP:
  NOT_YET
```

## Final Boundary

This coupled construction is a stronger internal stress test than the product
attempt because the linear part is genuinely coupled and the support is
connected. It still does not certify full `Crit_op` membership because `Iint`
is open. It is internal, non-canonical, speculative, and never external
validation. It is not a real-world `C_op` instance and introduces no new public
claim.
